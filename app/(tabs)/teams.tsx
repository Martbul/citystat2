import { Text } from "react-native";
import { View } from "react-native";

export default function TeamsScreen() {
  return (
    <View className="flex-1 justify-center items-center bg-lightSurface">
      <Text className="text-lightBlackText text-lg">Teams Screen</Text>
    </View>
  );
}  